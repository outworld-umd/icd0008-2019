@page
@model ExamApp.Pages_Choices.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Choice[0].Text)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Choice[0].Question)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Choice) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Text)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Question.Title)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ChoiceId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ChoiceId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ChoiceId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
